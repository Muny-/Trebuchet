length_counterweight_arm = ft(2.2);

length_projectile_arm = ft(6);

_length_arm = length_counterweight_arm + length_projectile_arm + ft(0.8);

inside_base_length = 120;

outside_base_width = 30;

_inside_base_width = outside_base_width-(1.5*2);

pivot_height = ft(6);
_pivot_beam_length = pivot_height+3.5+0.25+4;